/**
 * Types du module partage — à compléter.
 * Submission : typage commun du projet (pas de redéfinition locale).
 */
export type { Submission } from "@/types/community";

// TODO: types propres au partage (métadonnées, variantes, etc.)
