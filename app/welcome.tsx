import { LandingScreen } from '@/features/landing/LandingScreen';

/** Route marketing réservée — non branchée sur « / » pendant le développement. */
export default function WelcomeRoute() {
  return <LandingScreen />;
}
